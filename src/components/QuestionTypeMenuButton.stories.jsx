import React from "react";
import QuestionTypeMenuButton from "./QuestionTypeMenuButton";

const Template = (args) => <QuestionTypeMenuButton {...args} />;

export const Default = Template.bind({});

export default {
  title: "Buttons/Question Type Menu Button",
};
