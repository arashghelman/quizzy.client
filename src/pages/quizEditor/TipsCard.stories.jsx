import React from "react";
import TipsCard from "./TipsCard";

export default {
  title: "QuizEditor/TipsCard",
  component: TipsCard,
};

const Template = () => <TipsCard />;
export const Default = Template.bind({});
