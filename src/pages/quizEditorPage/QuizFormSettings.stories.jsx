import React from "react";
import QuizFormSettings from "./QuizFormSettings";

export default {
  title: "Pages/Quiz Editor/Forms/Quiz Form Settings",
  component: QuizFormSettings,
};

const Template = (args) => <QuizFormSettings {...args} />;

export const Default = Template.bind({});
