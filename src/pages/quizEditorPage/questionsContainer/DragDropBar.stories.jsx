import React from "react";
import DragDropBar from "./DragDropBar";

export default {
  title: "Pages/Quiz Editor/Other/Drag Drop Bar",
  component: DragDropBar,
};

const Template = (args) => <DragDropBar {...args} />;

export const Default = Template.bind({});
