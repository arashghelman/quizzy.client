import React from "react";
import OptionLabel from "./OptionLabel";

export default {
  title: "Pages/Quiz Editor/Tags & Labels/Option Label",
  component: OptionLabel,
};

const Template = (args) => <OptionLabel {...args} />;

export const Default = Template.bind({});
